# Binaries
bin/
/adc
/adc-linux-amd64
*.exe

# Claude AI artifacts — not part of the project
claude/
.claude/

# Config with real values (keep config.toml as template)
config/config.example.toml

# Docker compose env files — contain credentials, never committed.
# .env.example and .env.external-db.example ARE tracked, as templates.
deploy/docker/.env
deploy/docker/.env.external-db

# IDE
.idea/
.vscode/
*.swp
*.swo

# Test files — kept locally, not pushed to remote
*_test.go
test/

# OS
.DS_Store
Thumbs.db
